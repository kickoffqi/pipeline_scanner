name: L1-05 echo secrets demo
on: [push]
permissions:
  contents: read
jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Echo secret (bad)
        run: |
          echo "${{ secrets.MY_TOKEN }}"
