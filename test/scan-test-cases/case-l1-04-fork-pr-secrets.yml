name: Case L1-04 Fork PR Secrets Surface (FAIL/WARN)

on:
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  pr:
    runs-on: ubuntu-latest
    env:
      # Placeholder only (do not use real secrets)
      DUMMY_SECRET: ${{ secrets.DUMMY_SECRET }}
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "This references secrets in a PR workflow; should be flagged."
          echo "DUMMY_SECRET=${DUMMY_SECRET}"
