[project]
name = "pipeline-scanner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.2",
    "python-dotenv>=1.2.1",
]

[project.scripts]
dev = "flask --app wsgi.py run --debug --host 0.0.0.0 --port 5001"
